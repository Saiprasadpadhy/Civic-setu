# Server
PORT=5000
NODE_ENV=development

# MongoDB — use your own connection string; never commit real credentials
MONGODB_URI=mongodb://localhost:27017/civicsetu

# JWT — generate a strong random secret for production
JWT_SECRET=your-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# CORS — comma-separated allowed origins
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Gemini AI — backend only, never expose to React
GEMINI_API_KEY=AIzaSyAyMc44tusB8b2tEgGyqYNFUcg1bDh_pfE

GEMINI_MODEL=gemini-2.0-flash

# Set to true to use mock AI responses (tests/local without Gemini billing)
AI_MOCK_MODE=false
